# Tasks: Ingesta Gmail (BACKLOG #5)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1250–1450 (WU1 migration+gmail.py+tests ~470; WU2 gmail_client+almacenamiento+config ~165; WU3 documento_repo+estado_integracion (breaks #4) ~205; WU4 cli_gmail+integration tests+docs ~405) |
| 400-line budget risk | High — WU1 and WU4 each individually approach/exceed the 400-line budget, same shape as item #4's forecast |
| Chained PRs recommended | Yes |
| Suggested split | WU1 → WU2 → WU3 → WU4 (four PRs, strictly sequential) |
| Delivery strategy | ask-on-risk — this forecast flags risk, so chained delivery is a stop-and-ask, not a silent decision |
| Chain strategy | pending — orchestrator to ask user |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Migration 013 (`ETIQUETA_PROCESADO` + `UNIQUE(EmailId, HashContenido)`) + `gmail.py` (pure: query, parse, candidacy, hash, sanitize, path) + its adversarial unit suite | PR 1 | `pytest tests/unit/test_gmail.py` | `dotnet test SmartNet.Db.Runner.Tests` (`BaseDataTests` InlineData) against a fresh `fact_test_<id>` | Revert `013_*.sql`/`013_down.sql`; delete `gmail.py`, `test_gmail.py`, fixtures; revert `BaseDataTests.cs` |
| 2 | `gmail_client.py` (IO: labels/messages/attachments/modify) + `almacenamiento.py` (IO: contained write) + `config.py` Gmail env vars + `pyproject.toml` deps | PR 2 | No dedicated unit file (design routes their testing through WU4's fakes/`tmp_path`); smoke: `python -c "import smartnet_worker.gmail_client, smartnet_worker.almacenamiento"` | N/A — no network/disk call exercised standalone; real IO only proven once wired in WU4 | Delete `gmail_client.py`, `almacenamiento.py`; revert `config.py`/`pyproject.toml` diffs |
| 3 | `documento_repo.py` (cursor-based INSERTs) + `estado_integracion.py` generalized to required `nombre` (breaks #4's `cli_tipo_cambio.py` + its test) | PR 3 | `pytest tests/unit/test_documento_repo.py tests/unit/test_estado_integracion.py` | N/A — fake-cursor unit tests only, no real DB in this slice | Revert `documento_repo.py`, `estado_integracion.py`, `cli_tipo_cambio.py`, and their tests to pre-PR3 state |
| 4 | `cli_gmail.py` orchestrator, fixtures, `test_cli_gmail.py`, structural + real-DB integration test updates, `README.md` | PR 4 | `pytest tests/unit/test_cli_gmail.py tests/unit/test_no_dbo_structural.py` (unit); `pytest -m integracion` (real pyodbc) | Ephemeral `fact_test_worker_<id>` + real `CREATE LOGIN usr_worker`, same harness item #4's WU3/WU4 built | Delete `cli_gmail.py`, `test_cli_gmail.py`, fixtures used only here; revert structural/integration test + README diffs |

---

## Phase 1 (WU1): Migration + `gmail.py` pure module

- [x] 1.1 Create `SmartNet/db/schema/013_configuracion_etiqueta_procesado.sql` — seed `INGESTA.ETIQUETA_PROCESADO` (`NOT EXISTS`-guarded) + `ALTER TABLE fact.DocumentoRecibido ADD CONSTRAINT UQ_DocumentoRecibido_Email_Hash UNIQUE (EmailId, HashContenido)` (`IF NOT EXISTS`-guarded), per design.md's migration content. Also regenerated `checksums.txt` via `generate-checksums.ps1` (13 entries).
- [x] 1.2 Create `SmartNet/db/schema/rollback/013_down.sql` — `DELETE` the key + `DROP CONSTRAINT`, advisory "CANNOT UNDO" note per 009's pattern.
- [x] 1.3 Modify `SmartNet/db/runner/.../BaseDataTests.cs` — add `[InlineData("INGESTA", "ETIQUETA_PROCESADO")]`; run to confirm the seeded row has `Valor`/`ValorPorDefecto` both `NULL`. Evidence: `dotnet test SmartNet.Db.Runner.Tests --filter "FullyQualifiedName~BaseDataTests|FullyQualifiedName~ChecksumManifestTests"` → 38/38 passed (includes the new InlineData case and `RealManifest_MatchesTheRealScripts_Exactly`).
- [x] 1.4 Create `SmartNet/worker/tests/fixtures/gmail_mensaje_simple.json` and `gmail_mensaje_multipart.json` — redacted real-shape `messages.get` responses (nested multipart/alternative + multipart/related with an inline empty-filename image, and a flat one-attachment message). All addresses/ids/content are synthetic and redacted, per fixtures/README.md's existing convention.
- [x] 1.5 RED: `SmartNet/worker/tests/unit/test_gmail.py::test_construir_consulta_*` — label with spaces quoted, `after:aaaa/mm/dd` reformatting from ISO date. RED evidence: `ModuleNotFoundError: No module named 'smartnet_worker.gmail'` (collection error, all 37 tests in the file failed to collect).
- [x] 1.6 RED (same file): `parsear_mensaje` over both fixtures (nested multipart walk, `internalDate`→UTC, missing `From`→`ParseoGmailError`, `Asunto`>500 truncated). Same RED evidence as 1.5 (single collection failure covering the whole file).
- [x] 1.7 RED (same file): `extensiones_permitidas`/`es_candidato` table — `pdf`✓, `.PDF`✓, `docx`✗, `factura.pdf.exe`✗, no-extension✗, empty `filename`✗, list with spaces/dots/empties. Same RED evidence as 1.5.
- [x] 1.8 RED (same file): `calcular_hash` — known vector `sha256(b"")`, 64 lowercase hex chars. Same RED evidence as 1.5.
- [x] 1.9 RED (same file), adversarial: `sanitizar_nombre_archivo`/`ruta_relativa` — `../../etc/passwd`, `..`, `.`, `....`, `C:\x`, `a:b`, `CON.pdf`, `NUL`, 300-char name, emoji-only name, two same-name-different-content attachments → distinct paths; path ≤400, component ≤255. Same RED evidence as 1.5.
- [x] 1.10 Confirmed all of 1.5–1.9 RED: `pytest tests/unit/test_gmail.py -q` → `ImportError ... ModuleNotFoundError: No module named 'smartnet_worker.gmail'`, 1 error during collection (`gmail.py` did not exist yet).
- [x] 1.11 GREEN: created `SmartNet/worker/src/smartnet_worker/gmail.py` — `AdjuntoGmail`, `MensajeGmail` dataclasses; `construir_consulta`, `parsear_mensaje`, `extensiones_permitidas`, `es_candidato`, `calcular_hash`, `sanitizar_nombre_archivo`, `ruta_relativa`, `ParseoGmailError`, per Interfaces/Contracts. Zero IO/DB/network/clock — same purity discipline as `sbs.py`.
- [x] 1.12 Confirmed `pytest tests/unit/test_gmail.py -q` fully GREEN: `37 passed in 0.14s`. Also `ruff check src/ tests/` → `All checks passed!`, and full unit suite `pytest tests/unit -q -m "not integracion and not externa"` → `55 passed in 0.24s` (no regression in item #4's SBS/estado_integracion/structural tests).

## Phase 2 (WU2): Gmail/storage IO + config/deps

- [x] 2.1 Modify `SmartNet/worker/pyproject.toml` — added `google-api-python-client>=2.140`, `google-auth>=2.34` to `dependencies`; registered `smartnet-gmail = "smartnet_worker.cli_gmail:main"` under `[project.scripts]` (target module ships in WU4; `pip install -e ".[dev]"` does not validate entry-point targets at install time — confirmed by a clean install in this WU).
- [x] 2.2 Modify `SmartNet/worker/src/smartnet_worker/config.py` — added `GMAIL_CREDENTIALS_ENV_VAR` (`SMARTNET_WORKER_GMAIL_CREDENTIALS`), `STORAGE_ROOT_ENV_VAR` (`SMARTNET_WORKER_STORAGE_ROOT`), `GMAIL_SCOPES=["https://www.googleapis.com/auth/gmail.modify"]`, plus `obtener_credenciales_gmail_json()` (parses the env JSON, `ConfiguracionError` on missing/malformed) and `obtener_raiz_almacenamiento()` (`ConfiguracionError` on missing) — no default in code, same shape as the pre-existing `obtener_connection_string`. No dedicated `test_config.py` added: item #4 never unit-tested `obtener_connection_string`'s own error branch either (verified: no such file exists pre-WU2), so this mirrors the established convention rather than introducing a new one.
- [x] 2.3 Create `SmartNet/worker/src/smartnet_worker/gmail_client.py` — `ClienteGmail` with `resolver_etiquetas`, `buscar_mensajes` (paginated via `nextPageToken`), `obtener_mensaje`, `obtener_adjunto` (base64url decode), `aplicar_etiqueta`; one method per API call, zero decision/parsing beyond mechanical pagination looping (design Decision 1 & 3 — missing-label handling is the *caller's* job, not this class's). No dedicated unit test file, per design's Testing Strategy (exercised via WU4's fake `ClienteGmail`).
- [x] 2.4 Create `SmartNet/worker/src/smartnet_worker/almacenamiento.py` — `escribir(raiz, ruta_relativa, datos)` with `resolved.is_relative_to(raiz)` containment guard (`ContencionError`), defense in depth beyond `gmail.py`'s sanitizer. **Deviation from the WU2 forecast's "no dedicated unit file"**: the containment guard is genuine decision logic (raise vs. write), not pass-through IO, so strict TDD applies per this session's explicit instruction. RED: `SmartNet/worker/tests/unit/test_almacenamiento.py` written first → `ModuleNotFoundError: No module named 'smartnet_worker.almacenamiento'` (1 collection error). GREEN: `pytest tests/unit/test_almacenamiento.py -q` → `4 passed` (happy path with intermediate dirs, idempotent same-path rewrite, relative-escape rejection, absolute-path-outside-root rejection — none of the negative cases write anything outside the root).
- [x] 2.5 Smoke check: both new modules import cleanly after `pip install -e ".[dev]"` pulled the new Gmail deps — `python -c "import smartnet_worker.gmail_client, smartnet_worker.almacenamiento"` → `ok`. `ruff check src/ tests/` → `All checks passed!`. Full non-integration unit suite: `pytest tests/unit -q -m "not integracion and not externa"` → `59 passed` (55 from WU1 + 4 new `test_almacenamiento.py`, no regression).

## Phase 3 (WU3): `documento_repo.py` + `estado_integracion.py` generalization (breaks item #4)

- [x] 3.1 RED: `SmartNet/worker/tests/unit/test_documento_repo.py` — `insertar_email(cursor, m, fecha_deteccion)` returns an int id on success, `None` on fake `IntegrityError` (idempotency gate); exact SQL text + parameters, `fact.` qualified, `'CANDIDATO'` literal. RED evidence: `ModuleNotFoundError: No module named 'smartnet_worker.documento_repo'` (1 collection error, all 13 tests in the file failed to collect).
- [x] 3.2 RED (same file): `insertar_documento(cursor, email_id, m, a, hash_hex, ruta_relativa)` — exact SQL/params, `'DESCARGADO'` literal, duplicate `(EmailId, HashContenido)` `IntegrityError` treated as no-op (design Decision 4). Same RED evidence as 3.1 (single collection failure covering the whole file).
- [x] 3.3 Confirmed 3.1–3.2 RED: `pytest tests/unit/test_documento_repo.py -q` → `ModuleNotFoundError: No module named 'smartnet_worker.documento_repo'`, 1 error during collection (`documento_repo.py` did not exist yet).
- [x] 3.4 GREEN: created `SmartNet/worker/src/smartnet_worker/documento_repo.py` — `insertar_email`, `insertar_documento` per Interfaces/Contracts; catches `pyodbc.IntegrityError` on both inserts (never a pre-`SELECT`); `insertar_email` reads the generated id via `SELECT SCOPE_IDENTITY()` on the same cursor after a successful insert, and skips that second `execute` entirely on `IntegrityError`. `NombreArchivo` truncated to 255 at the write site (Decision 5). GREEN: `pytest tests/unit/test_documento_repo.py -q` → `9 passed in 0.06s`.
- [x] 3.5 RED: modified `SmartNet/worker/tests/unit/test_estado_integracion.py` — `registrar_exito`/`registrar_fallo` now require `nombre: str` as the second positional argument; added a `Nombre='GMAIL'` case and a name-outside-`CK_EstadoIntegracion_Nombre` case (both still raise via `rowcount != 1`). RED evidence: `pytest tests/unit/test_estado_integracion.py -q` → `8 failed` with `TypeError: registrar_exito() takes 2 positional arguments but 3 were given` / same for `registrar_fallo`, confirming the fails were against the old `Nombre='SBS'`-hardcoded signature (not a collection error — the module still imported fine).
- [x] 3.6 GREEN: modified `estado_integracion.py` — `nombre` is now a required, parameterized (`WHERE Nombre = ?`) argument to both `registrar_exito(cursor, nombre, instante)` and `registrar_fallo(cursor, nombre, instante, error)`; `_verificar_una_fila_afectada` takes `nombre` too so the raised message names which integration failed to update.
- [x] 3.7 GREEN: modified `SmartNet/worker/src/smartnet_worker/cli_tipo_cambio.py` — passes `'SBS'` explicitly at both call sites (`registrar_exito(cursor, "SBS", instante)`, `registrar_fallo(cursor, "SBS", instante, error)`), consequence of 3.6's new signature.
- [x] 3.8 Confirmed GREEN, no regression in item #4's SBS path: `pytest tests/unit/test_documento_repo.py tests/unit/test_estado_integracion.py -q` → `17 passed`. No dedicated `test_cli_tipo_cambio.py` exists (item #4 never added one, same convention noted in WU2's 2.2 for `config.py`); its two call sites are exercised transitively via the `estado_integracion` signature change compiling/running clean. Full non-integration unit suite: `pytest tests/unit -q -m "not integracion and not externa"` → `70 passed` (59 pre-WU3 + 9 `test_documento_repo.py` + 2 net-new `test_estado_integracion.py` cases). `ruff check src/ tests/` → `All checks passed!`.

## Phase 4 (WU4): `cli_gmail.py` orchestrator + integration + docs

- [x] 4.1 RED: `SmartNet/worker/tests/unit/test_cli_gmail.py` — fake `ClienteGmail` + fake cursor/conexion (`_ClienteGmailFalso`, `_FakeCursor`, `_FakeConexion`, injected via `ejecutar(cliente=..., conectar=...)`): label applied only after commit; failed message left unlabeled and does not abort the run; message with no candidate attachments → 0 rows/0 writes/no label; `insertar_email → None` → no download, label reapplied (idempotent re-run); missing `ETIQUETA_PROCESADO` at Gmail → fails before `buscar_mensajes` (`messages.list`); plus real `tmp_path` write-through and a missing-`fact.Configuracion`-value guard. RED evidence: `ModuleNotFoundError: No module named 'smartnet_worker.cli_gmail'` (1 collection error, all 10 tests in the file failed to collect) — `cli_gmail.py` moved aside to `cli_gmail.py.bak` before writing the test, confirmed the failure, then restored.
- [x] 4.2 Confirmed 4.1 RED: `pytest tests/unit/test_cli_gmail.py -q` → `ModuleNotFoundError: No module named 'smartnet_worker.cli_gmail'`, 1 error during collection.
- [x] 4.3 GREEN: created `cli_gmail.py` — `ejecutar(*, cliente=None, conectar=pyodbc.connect, instante=None)`: env config → `fact.Configuracion` read (own transaction, before any Gmail call) → `ClienteGmail` (or injected fake) → `resolver_etiquetas` (fails loud if either label id missing, design Decision 3) → `buscar_mensajes` → per-message transaction (`insertar_email` → download/hash/write/`insertar_documento` if not `None` → `COMMIT`) → `aplicar_etiqueta` (only after commit, outside the transaction) → per-message failure isolation (`except Exception` in the loop) → one final `registrar_exito`/`registrar_fallo` (design Decision 7). `cliente`/`conectar` are the two seams design.md's table names, defaulted to real IO, substitutable in tests without touching any other line.
- [x] 4.4 Confirmed GREEN: `pytest tests/unit/test_cli_gmail.py -q` → `9 passed` (one assertion tightened after first GREEN run: "no per-message commit for a no-candidate message" corrected to "exactly one commit — the final EstadoIntegracion one" once the real control flow was observed).
- [x] 4.5 RED→GREEN: modified `SmartNet/worker/tests/unit/test_no_dbo_structural.py` — split into three tests sharing `_archivos_fuente()`: existing `dbo.` scan (unchanged), new `.delete(`/`.trash(` scan, new `fact.factura`/`fact.adjuntomanual`/`fact.procesamiento`/`fact.datosextraidos` scan, all over the complete `src/smartnet_worker/*.py`. First run caught a real false positive: `gmail_client.py`'s own docstring *mentioned* `` `.delete(`/`.trash(` `` in prose (documenting the guard), which the literal scanner (correctly) flagged — fixed by rewording the docstring to describe the rule without repeating the banned substrings, not by weakening the scanner.
- [x] 4.6 Confirmed GREEN: `pytest tests/unit/test_no_dbo_structural.py -q` → `3 passed`.
- [x] 4.7 Modified `SmartNet/worker/tests/integration/test_pyodbc_integracion.py` (marker `integracion`) — added `test_insertar_email_real_inserta_y_el_duplicado_devuelve_none`, `test_insertar_documento_real_referencia_el_email_por_fk`, `test_registrar_exito_con_nombre_gmail_afecta_exactamente_una_fila`, `test_usr_worker_no_puede_escribir_en_configuracion` (negative, expects `pyodbc.Error` under `usr_worker`'s SELECT-only grant, 008). Also fixed a pre-existing broken call site in this same file: `registrar_exito(cursor, instante)` (item #4's original 2-arg call) never got the `nombre` parameter WU3 made required — updated to `registrar_exito(cursor, "SBS", instante)`.
- [x] 4.8 Ran for real: SQL Server and `dotnet` WERE reachable in this environment (unlike WU1-WU3's implementation environment). First run of the new Gmail tests found a real bug: `insertar_email`'s `SELECT SCOPE_IDENTITY()` in a *separate* `cursor.execute()` after the INSERT returned `NULL` — pyodbc wraps a parameterized INSERT in `sp_executesql`, which closes its own scope on return, so a later statement's `SCOPE_IDENTITY()` sees nothing. Fixed `documento_repo.insertar_email` to use `OUTPUT INSERTED.EmailId` in the SAME `execute` as the INSERT (removed `_SELECT_SCOPE_IDENTITY` entirely), updated its unit test (`test_insertar_email_lee_el_id_con_output_en_el_mismo_execute_que_el_insert`, was asserting a 2nd execute that no longer exists) and `test_cli_gmail.py`'s fake cursor. Re-ran: `pytest -m integracion -q` → `7 passed in 49.73s` against a real ephemeral `fact_test_worker_<id>` database + real `usr_worker` login (full versioned schema applied via `SmartNet.Db.Runner`, migration 013 included). Confirmed zero orphaned resources: `sqlcmd -Q "SELECT name FROM sys.databases WHERE name LIKE 'fact_test_worker_%'; SELECT name FROM sys.server_principals WHERE name='usr_worker'"` → both `(0 rows affected)`.
- [x] 4.9 Modified `SmartNet/worker/README.md` — documented `SMARTNET_WORKER_GMAIL_CREDENTIALS` and `SMARTNET_WORKER_STORAGE_ROOT` (both no-default-in-code, per design Decision 2/ADR 0013), the `smartnet-gmail` command and `python -m smartnet_worker.cli_gmail`, updated the package-level description to cover both CLIs, and added a "Limitaciones conocidas" entry documenting the real integration run and the `SCOPE_IDENTITY()`→`OUTPUT` bugfix from 4.8.
- [x] 4.10 Confirmed by inspection: `.github/workflows/ci.yml` needs no change. `verificaciones-estaticas` runs `pytest -m "not integracion and not externa"` with no filename list (auto-discovers `tests/`, now including `test_cli_gmail.py` and the extended structural test); `pruebas-de-worker-python` runs `pytest -m integracion` the same way (now including the four new Gmail cases in `test_pyodbc_integracion.py`); `SmartNet.Db.Runner` applies every `schema/*.sql` in lexical order with no hardcoded script list, so `013_configuracion_etiqueta_procesado.sql` is picked up automatically. No diff made.
- [x] 4.11 `ruff check src/ tests/` → `All checks passed!` (full worker package, post-fix). Full non-integration unit suite: `pytest -q -m "not integracion and not externa"` → `81 passed, 7 deselected`.

### Work Unit Evidence (WU4)

| Evidence | Value |
|---|---|
| Focused test command and exact result | `pytest tests/unit/test_cli_gmail.py tests/unit/test_no_dbo_structural.py -q` → `12 passed` |
| Runtime harness command/scenario and exact result | `pytest -m integracion -q` against a real ephemeral SQL Server (`fact_test_worker_<id>` + real `usr_worker` login, full versioned schema via `SmartNet.Db.Runner`) → `7 passed in 49.73s`; zero orphaned DBs/logins confirmed via `sqlcmd` |
| Rollback boundary | Delete `cli_gmail.py`, `tests/unit/test_cli_gmail.py`; revert `tests/unit/test_no_dbo_structural.py`, `tests/integration/test_pyodbc_integracion.py`, `README.md`, `documento_repo.py` (OUTPUT fix), `gmail_client.py` (docstring wording), `tests/unit/test_documento_repo.py` to their pre-WU4 state |

This closes item #5 (BACKLOG Ingesta Gmail): 36/36 tasks complete across WU1-WU4.
