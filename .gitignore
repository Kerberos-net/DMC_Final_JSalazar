# Configuracion de herramientas de asistencia, no del proyecto
.claude/
.agents/
.atl/

# Temporales de Office al abrir los datos maestros
~$*.xlsx

# .NET build output
bin/
obj/
*.user
.vs/

# Python build output / virtualenvs
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc
SmartNet/SmartNetWorker/build/
SmartNet/SmartNetWorker/dist_wheel/
.claude/.lecciones-aprendidas-seen

# Deploy: config y secretos por entorno viven SOLO en el host, nunca en el repo
deploy/config.*.ps1
!deploy/config.example.ps1
*.env

# Indices y estado local de herramientas de asistencia (se regeneran)
.codegraph/
.engram/
Arquitectura SmartNet.png

# Paquetes de release compilados localmente
/dist/
